import React,{useEffect,useState} from 'react';import {api} from '../lib/api';export default function Automations(){const[d,setD]=useState(null);useEffect(()=>{api.automations().then(setD)},[]);return <main><div className="topbar"><div><div className="brand">AUTOMATIONS</div><p>Read-only control-center view of Baby's automation store.</p></div></div>{d?.status!=='READY'?<div className="notice">Automation database not found.</div>:<pre className="dataDump">{JSON.stringify(d,null,2)}</pre>}</main>}
