import React,{useEffect,useState} from 'react';import {api} from '../lib/api';
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api.control().then(setD)},[]);return <main><div className="topbar"><div><div className="brand">BABY <span>CONTROL CENTER</span></div><p>Investor, validation, paper portfolio, automations and alerts in one place.</p></div></div><div className="panelGrid">{Object.entries(d?.components||{}).map(([k,v])=><div className="controlCard" key={k}><small>{k.toUpperCase()}</small><strong>{v.available?'CONNECTED':'NOT AVAILABLE'}</strong><p>{v.path}</p></div>)}</div><div className="authority"><b>Execution safety</b><span>AI scoring authority 0% · AI execution authority 0% · real-money execution DISABLED</span></div></main>}
