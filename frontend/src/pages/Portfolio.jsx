import React,{useEffect,useState} from 'react';import {api} from '../lib/api';export default function Portfolio(){const[d,setD]=useState(null);useEffect(()=>{api.paper().then(setD)},[]);return <main><div className="topbar"><div><div className="brand">PAPER PORTFOLIO</div><p>Simulation only. No brokerage execution.</p></div></div><div className="authority"><b>REAL MONEY DISABLED</b><span>Orders shown here are paper-trading records only.</span></div>{d?.status!=='READY'?<div className="notice">Paper-trading database not found yet.</div>:Object.entries(d).filter(([k,v])=>Array.isArray(v)).map(([k,v])=><section className="stage" key={k}><h2>{k}</h2><div className="tableWrap"><table><thead><tr>{Object.keys(v[0]||{}).map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{v.slice(0,50).map((r,i)=><tr key={i}>{Object.values(r).map((x,j)=><td key={j}>{String(x??'')}</td>)}</tr>)}</tbody></table></div></section>)}</main>}
